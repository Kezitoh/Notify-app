import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aldedu.notify',
  appName: 'Notify-App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
